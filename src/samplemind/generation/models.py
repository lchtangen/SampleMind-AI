"""GenerationRequest and GenerationResult data models.

Phase 16 — AI Generation.
GenerationRequest: prompt, duration_seconds, bpm (optional), key (optional),
  style hints, backend name (audiocraft | stable_audio | mock).
GenerationResult: output_path, duration_seconds, backend_used, sample_id
  (set after auto-analysis and library upsert).
"""
# TODO: implement in Phase 16 — AI Generation
