"""Audio and text embedding generation via CLAP (sentence-transformers).

Phase 11 — Semantic Search.
Wraps a CLAP model to produce 512-dimensional float32 embeddings for both
audio files (embed_audio) and text queries (embed_text). Used by the
vector index builder and the CLI similar command.
"""
# TODO: implement in Phase 11 — Semantic Search
