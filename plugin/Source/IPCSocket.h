#pragma once
// Phase 8 — VST3/AU Plugin
// IPCSocket: thin wrapper around a Unix domain socket client.
// connect() opens ~/tmp/samplemind.sock. send_request() writes a
// length-prefixed JSON message and reads the length-prefixed response.
// Protocol version: 2 (matches sidecar/protocol.py envelope format).
// TODO: implement in Phase 8 — VST3/AU Plugin
